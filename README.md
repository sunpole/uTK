# uTK

Python and PowerShell production helpers. `extract_tk.py` extracts order details from PDFs with OCR, while the folder scripts create and organise order folders.

The OCR utility uses EasyOCR and PyMuPDF.
